// This file was auto-generated by Fern from our API Definition.

import { Hono, type MiddlewareHandler } from "hono";
import { setCookie } from "hono/cookie";
import type { CookieOptions } from "hono/utils/cookie";
import * as errors from "../../../../errors/index";
import type * as SeedApi from "../../../index";

export interface TestGroupServiceMethods {
    /**
     * Post a nullable request body
     */
    testMethodName(
        req: {
            params: {
                path_param: string;
            };
            query: {
                query_param_object?: SeedApi.PlainObject | null;
                query_param_integer?: number | null;
            };
            body: SeedApi.PlainObject | null;
        },
        res: {
            send: (responseBody: unknown) => Promise<void>;
            cookie: (cookie: string, value: string, options?: CookieOptions) => void;
            locals: any;
        },
    ): void | Promise<void>;
}

export class TestGroupService {
    private app;

    constructor(
        private readonly methods: TestGroupServiceMethods,
        middleware: MiddlewareHandler[] = [],
    ) {
        this.app = new Hono();
        middleware.forEach((m) => this.app.use("*", m));
    }

    /** Adds middleware to the Hono app for all routes. */
    public addMiddleware(handler: MiddlewareHandler): this {
        this.app.use("*", handler);
        return this;
    }

    /** Returns the Hono app instance with all routes configured. */
    public toHono(): Hono {
        this.app.post("/optional-request-body/:path_param", async (c) => {
            const request = await c.req.json();
            if (request.ok) {
                try {
                    let _responseBody: unknown | undefined;
                    const _cookies: {
                        name: string;
                        value: string;
                        options?: CookieOptions;
                    }[] = [];
                    await this.methods.testMethodName(
                        {
                            params: c.req.param(),
                            query: c.req.query(),
                            body: request.value,
                        },
                        {
                            send: async (responseBody) => {
                                _responseBody = responseBody;
                            },
                            cookie: (name, value, options) => {
                                _cookies.push({ name, value, options });
                            },
                            locals: {},
                        },
                    );
                    for (const cookie of _cookies) {
                        setCookie(c, cookie.name, cookie.value, cookie.options);
                    }
                    return c.json(_responseBody, 200);
                } catch (error) {
                    if (error instanceof errors.SeedApiError) {
                        switch (error.errorName) {
                            case "UnprocessableEntityError":
                                break;
                            default:
                                console.warn(
                                    `Endpoint 'test_method_name' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                                );
                        }
                        return error.toResponse(c);
                    } else {
                        return c.json("Internal Server Error", 500);
                    }
                }
            } else {
                return c.json(
                    {
                        errors: request.errors.map(
                            (error) => `${["request", ...error.path].join(" -> ")}: ${error.message}`,
                        ),
                    },
                    422,
                );
            }
        });
        return this.app;
    }
}
